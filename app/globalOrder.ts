/**
 * Global image order used for all visitors as the default gallery order.
 *
 * Manual sync flow:
 * 1) Reorder images in the UI.
 * 2) Click "Make Global Default".
 * 3) Paste exported JSON into this file and deploy.
 */
export const GLOBAL_IMAGE_ORDER: string[] = [];
